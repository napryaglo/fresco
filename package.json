{
  "name": "fresco",
  "version": "0.1.0",
  "description": "Graph visualization application built on top of mural",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "ge": "tsx src/ge/main.ts",
    "test": "tsx --test \"src/**/*.test.ts\"",
    "clean": "rimraf dist"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.10.0",
    "rimraf": "^6.0.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@visualisation-sub/mural": "^0.1.0",
    "yaml": "^2.9.0"
  }
}
